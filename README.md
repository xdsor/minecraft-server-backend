## .cfg example

```
SERVER_HOST = "example.com"                         # used by RCON client
RCON_PASSWORD = "password"                          # RCON client password
MINECRAFT_SERVER_PATH = "/mnt/minecraft-server      # Used to access server's files like logs and world information
```